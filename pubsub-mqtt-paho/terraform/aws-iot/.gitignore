# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan

# Certificates and keys - NEVER commit these!
certificates/
*.pem
*.crt
*.key

# Terraform variable files that might contain secrets
*.tfvars
!example.tfvars
